# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0               # async PostgreSQL driver
psycopg2-binary==2.9.9        # sync driver for Alembic migrations
aiosqlite==0.22.1              # async SQLite driver for local development fallback

# Data Validation
pydantic==2.7.1
pydantic-settings==2.2.1

# File Handling
python-multipart==0.0.9       # multipart form uploads

# PDF Handling (validation only for now)
pypdf==4.2.0                  # PDF reading + encryption detection
pdfplumber==0.11.0             # text + table extraction (future parsing)
python-magic-bin==0.4.14       # MIME type detection (bundles libmagic DLLs for Windows)

# CSV / Data
chardet==5.2.0                # encoding detection
pandas==2.2.2                 # chunked CSV reading
openpyxl==3.1.2               # Excel → CSV conversion

# Date & Amount Parsing
python-dateutil==2.9.0        # robust date parsing
babel==2.14.0                 # locale-aware number parsing

# Async / Background Jobs
celery==5.4.0                 # background workers (wired but optional)
redis==5.0.4                  # Celery broker

# Logging & Observability
structlog==24.1.0             # structured JSON logging
python-json-logger==2.0.7

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                 # async test client for FastAPI
pytest-cov==5.0.0

# Utilities
python-dotenv==1.0.1
uuid6==2024.1.12
